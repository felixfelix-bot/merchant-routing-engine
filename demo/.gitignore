# Demo runtime state (token-ledger DB, participant data) — never commit
*.db
*.db-shm
*.db-wal
demo-ledger.db

# Node
node_modules/

# Local env overrides
.env
.env.*

# CVM server private key (defense-in-depth — never commit)
cvm-server-key.json
